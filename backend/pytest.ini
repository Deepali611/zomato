[pytest]
testpaths = tests
pythonpath = .
addopts = -ra --strict-markers
markers =
    integration: integration tests with fixture data (default suite)
    e2e: end-to-end tests requiring live Groq API key and parquet data
